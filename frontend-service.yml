apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    name: frontend-service
    app: pyredisapp
spec:
  ports:
  - port: 80
    targetPort: 8082
  selector:
    name: pyapp-pod
    app: pyredisapp
  type: LoadBalancer